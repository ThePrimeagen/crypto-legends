package main

func main() {
    /*
	irc_client := twitch.CreateIrcClient()
	irc_client.AddListener(func(name string, message string) {
		log.Printf("irc %s: %s\n", name, message)
	})

	client := coinbasepro.NewClient()
	book, err := client.GetBook("BTC-USD", 1)
	if err != nil {
		log.Fatalf("%+v\n", err)
	}

	timer1 := time.NewTimer(2 * time.Second)

	go func() {
		<-timer1.C
		log.Printf("Look we got a C\n\n\n")
	}()

	log.Printf("Look %+v\n", book)

	time.Sleep(15 * time.Second)
    */
}
